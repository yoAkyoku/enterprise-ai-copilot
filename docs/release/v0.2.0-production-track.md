# v0.2.0 production-track checklist

Status: implementation in progress. This document is a gate, not a claim that
production deployment has already occurred.

## Included in this track

- Tenant-scoped Web operations console served by the API.
- Image evidence upload, content validation, preview, deletion and audit.
- Pluggable pre-storage malware scanner boundary, ClamAV INSTREAM adapter and
  retention cleanup API.
- PostgreSQL attachment metadata, audit records, approvals and restart-safe run
  records for the production Compose profile; SQLite remains the local and
  explicit single-node adapter.
- Workspace- and tenant-filtered audit reads, including legacy-row fail-closed behavior.
- Signed HS256 JWT boundary for self-hosted production identity injection.
- OIDC/JWKS RS256 boundary with exact HTTPS host allowlisting and key rotation
  refresh on unknown `kid`.
- Fail-closed rejection of preview auth modes and synthetic providers in
  staging and production startup.
- Migration files for runs, audit events and attachments.
- Durable approval queue with expiry, scope, argument hash and one-time token
  issuance.
- Authenticated metrics and readiness endpoints with request ID propagation.
- PostgreSQL migration and custom-format backup helpers; production
  backup/restore still needs execution against the target database and storage
  service.
- OpenAI-compatible Vision/OCR adapter with HTTPS host allowlisting, bounded
  response, timeout and explicit per-request external-processing consent.
- OpenAI-compatible text-model adapter with bounded evidence input, HTTPS host
  allowlisting, explicit external-processing consent and unverified-output
  labeling.
- Redis-backed atomic rate limiting is selected when `AGENT_REDIS_URL` is set;
  production startup rejects the in-process fallback.
- A production Compose profile runs API, PostgreSQL migration, continuous Agent
  worker, scheduler and Redis with read-only container filesystems and restart
  policies.
- S3-compatible object storage is available with server-side encryption and
  bounded reads; production startup rejects the local filesystem adapter.
- The tag workflow includes a fail-closed `scripts/release_gate.py` check for
  resolved checklist items, PASS/WAIVED evidence and commit-bound records, plus
  a stable package-version/tag consistency check.
- A protected, manually triggered production-acceptance workflow exercises the
  real target API with two OIDC principals and redacted provider/attachment
  evidence; it never runs automatically for pull requests.

## Current release-track update: 2026-08-21 (protected acceptance path)

- The repository now includes `scripts/api_acceptance_smoke.py` and the
  manual `.github/workflows/production-acceptance.yml` workflow. It uses two
  real bearer principals, a reviewed test order, bounded provider probes and
  synthetic attachment fixtures that are deleted before completion.
- The live connector smoke now imports its request contract correctly and uses
  the reviewed `AGENT_ACCEPTANCE_ORDER_ID` fixture when supplied; the local
  fake-client acceptance test covers owner access, cross-scope rejection and
  cleanup without contacting external services.
- The protected target workflow itself is `NOT_RUN` because this checkout has
  no target credentials or deployment. The six formal target-environment gates
  remain open and no stable versioned release is authorized.

## Current hosted validation update: 2026-08-21 (protected acceptance path)

- Commit `dcd61b2cbece5fdbedf459224124bd880a410522` passed hosted CI run
  `32457142256`: validate `96696499134`, Redis worker smoke `96696499017`,
  CodeQL `96696498819` and container smoke `96696648000`.
- The hosted artifact `9437630755` was retained. The protected live acceptance
  workflow itself was not invoked, so the six target-environment gates remain
  open and no versioned release is authorized.

## Formal production gates still required

- [x] Configure and validate an external OIDC/JWKS identity provider or
      complete a documented key-rotation process for HS256.
- [ ] Replace synthetic ERP/MCP/model adapters with reviewed connectors and
      run their integration and failure-mode tests.
- [ ] Run S3 object-storage encryption, backup/restore, scanner and retention
      evidence in the target deployment.
- [ ] Run a configured Vision/OCR provider integration with egress policy,
      consent, timeout, redaction and provenance tests.
- [ ] Validate Redis queue restart, retry, lock/claim and cancellation behavior
      in the target worker deployment, and add trace export.
- [x] Run Docker Compose and browser E2E checks on a host with the required
      runtime; record exact environment and artifacts.
- [ ] Run secret scans on committed Git history, dependency audit and CI on a
      tagged commit.
- [x] Review threat model, privacy/retention policy, incident response and
      migration rollback procedure.
- [ ] Commit, push and publish a versioned GitHub release only after all
      required gates are `PASS` or explicitly `WAIVED`.

## Current local evidence

The current checkout has 83 local unit/contract tests passing (plus the opt-in
PostgreSQL integration test when a disposable database is configured). The recorded
Playwright Chromium console flow at commit
`5f3910aea8b54fe608fe7dc8d0d9cf7356bd2c4d` covered bearer connect, order-status
run, image upload/preview and delete confirmation. The latest hosted run also
passed real Redis queue smoke, scheduler contracts, CodeQL and container smoke
using synthetic provider data and a development identity; target worker
deployment and all external provider/target-environment gates remain `NOT_RUN`
or `UNVERIFIED`.

The current release-track CI run `32442936627` at commit
`98bf033f655ebd30776dd3e5b136f087d3b4dc9e` also passed validate, Redis worker,
CodeQL and container-smoke/Trivy jobs. It produced the SPDX release artifact
`9433062468`; this still does not satisfy the six target-environment gates.

The latest release-track CI run `32445379295` at commit
`d12a087b69496a4709a68cb9fbc2ece320cb73a7` passed validate, Redis worker,
CodeQL and container smoke. It also verifies the production image asset
boundary and the pinned Ruff toolchain. This improves release reproducibility
but does not close the six target-environment gates above.

The latest local persistence commit is `ee79a1524c8f7b1b4ac8f3b8e65f216713d8dbcb`.
It adds the PostgreSQL adapters, migration gate, readiness probes, opt-in
integration test and custom-format backup helper. A disposable PostgreSQL
container passed migration/store/idempotency/restore smoke, and the production
image passed its in-image asset/import check. Hosted CI for this commit and the
host wrapper invocation of `pg_dump` are separate evidence boundaries. Hosted
CI run `32449172148` passed validate, CodeQL, Redis worker smoke and container
smoke; the host wrapper invocation of `pg_dump` remains `NOT_RUN` because the
development host does not provide that executable.

The latest tenant-scope commit is `128296424a8409f13c7379fba61aeb580d266bb9`.
It adds database-level tenant predicates and legacy-schema migration coverage
for audit events, updates the UI and public architecture description, and was
pushed to both `main` and `codex/production-release`. Local verification passed
78 tests, a disposable PostgreSQL run with five migrations and isolated
backup/restore, and production image asset/import checks. GitHub Actions run
`32451104583` passed validate `96679656392`, Redis worker `96679656340`, CodeQL
`96679656263` and container smoke `96679769463`. These are reproducible local
and synthetic hosted checks; the six target-environment gates above remain
open and no versioned GitHub release has been published.

The follow-up commit `d9f528d7a16c2c3665f17d51aa7a076fa0c190c0` makes the
release-gate ancestry check portable across local owner-mismatch environments
by passing an explicit Git safe-directory setting. Its 78-test local suite
passes, and the release gate now reports only the six unresolved formal target
gates above.

## Current release-track update: 2026-08-21 (connector provenance and readiness)

- Code commit: `e01b09b06b19ec028c0e7b7b6dc74ef3cbd6b82a`, pushed to `main` and
  `codex/production-release`.
- Local suite: `83` tests passed with one opt-in PostgreSQL integration test
  skipped; Ruff check/format, compileall, repository validation, doctor,
  committed-history secret scan and production Compose config passed.
- The remote MCP adapter now rejects mismatched workspace/tenant scope and
  record identity before success; its readiness probe accepts valid POST-only
  HTTP 405 responses while keeping auth and transport failures unhealthy.
- `/ready` now probes attachment metadata, blob storage and the configured
  malware scanner. Production preflight `--live` adds authenticated model/
  trace probes and a read-only S3 `HeadBucket` check with bounded retries.
- GitHub Actions run `32453472156` passed: validate `96686162328`, Redis worker
  smoke `96686162226`, CodeQL `96686162397` and container smoke `96686291474`.
  Release artifact `9436432361` was uploaded. These remain local/synthetic
  and hosted CI evidence; the six target-environment gates remain open.

## Current release-track update: 2026-08-21 (release hardening)

- Code commit: `5be8584af739235702fabd2e779cdaf3b3b9d1b7` on
  `codex/production-hardening`; it has not yet been promoted to `main`.
- Local validation passed `89` tests with one opt-in PostgreSQL test skipped,
  Ruff check/format, compileall, manifest validation, doctor, committed-history
  secret scan, production Compose configuration and `git diff --check`.
- Connector-controlled provenance/status fields are now length-bounded, and
  the release workflow fails closed unless a stable tag exactly matches
  `pyproject.toml`'s package version. Since the package remains `0.2.0.dev0`,
  the release-version guard correctly rejects `v0.2.0` and no release tag was
  created.
- The release gate still reports exactly six unresolved target-environment
  items. Hosted CI for this commit and all target OIDC, ERP/MCP, model,
  Vision/OCR, S3, ClamAV, OTLP, backup/restore, failover and production
  deployment evidence remain `NOT_RUN` or `UNVERIFIED`.

## Current hosted validation update: 2026-08-21

- GitHub Actions run `32455021819` for commit
  `5644d8eb1c06346c421529daa923ff4ad11a173a` completed successfully.
  Validate `96690535071`, Redis worker smoke `96690534785`, CodeQL
  `96690534958` and container smoke `96690712916` all passed; release artifact
  `9436961479` was retained.
- This hosted result is recorded as CI evidence only. It does not close the
  six target-environment gates or authorize a versioned release.
