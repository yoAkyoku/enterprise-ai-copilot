# Local validation record

- Date: 2026-08-21
- Scope: local production-track workspace
- OS: Windows
- Runtime: Python 3.13.12
- Provider mode: synthetic in-memory gateway; no external model or MCP server
- Database / queue: local SQLite used for audit, runs, approvals and attachment
  metadata; queue adapter contract tested with an in-memory queue, no external
  Redis worker run
- Git commit: `932e44ee180f35cddb8e94ed673fae1a8bf74b36`; `main` and
  `codex/production-release` have been pushed to the configured public GitHub
  repository

## Commands

```text
python -m compileall -q packages services tests scripts apps
ruff check packages services tests scripts
ruff format --check packages services tests scripts
python -m unittest discover -s tests -v
python -m services.api.main --order-id SO-1001
python -m services.cli validate
python -m services.cli doctor
python scripts/check_secrets.py
python scripts/check_secrets.py --git-history
python scripts/release_gate.py  # NOT_RUN: production checklist is intentionally open
python -m pip check
python -m pip wheel --no-cache-dir --no-deps --no-build-isolation --wheel-dir dist .
python -m pip install --no-cache-dir --no-deps --target .artifact-smoke dist/enterprise_ai_copilot-0.2.0.dev0-py3-none-any.whl
.\.venv-release\Scripts\python.exe -m pip install ".[dev,security,production]"
.\.venv-release\Scripts\python.exe -m pip check
.\.venv-release\Scripts\python.exe -m ruff check packages services tests scripts
.\.venv-release\Scripts\python.exe -m unittest discover -s tests -v
.\.venv-release\Scripts\python.exe -m pip_audit
```

## Results

- Compile check: exit code `0`.
- Ruff check: exit code `0`.
- Ruff format check: exit code `0` (52 Python files already formatted).
- Unit and contract suite: `59` tests, all passed, exit code `0` in the host
  environment; the GitHub Actions Python 3.12 validation job also passed.
- Direct scheduled-Agent smoke: the reviewed order-status schedule executed
  through the Agent Runtime with a deployment-controlled development identity,
  returned `succeeded` with one attempt, exit code `0`.
- CLI smoke test: synthetic `SO-1001` returned `succeeded`, included a trace
  ID, source reference and observed time, exit code `0`.
- Working-tree and committed-history secret scans: no configured secret
  patterns found, exit code `0`.
- Manifest validation, doctor and configured working-tree secret scan: exit
  code `0`.
- Clean release venv resolved runtime, dev, security and production extras
  (`Pillow`, `python-multipart`, `cryptography`, `redis`, `boto3`, `httpx2`,
  Ruff); `pip check` reported no
  broken requirements, exit code `0`.
- Local HTTP smoke: `/` and `/assets/app.css` returned `200`; authenticated
  dashboard returned `200`; the supplied PNG completed upload (`201`), list,
  content read and delete (`204`) with audit events.
- Final authenticated HTTP smoke on `127.0.0.1:8766`: `/`, CSS, `/health`,
  `/ready`, dashboard and `/metrics` returned `200`; the supplied PNG completed
  `201 upload`, list, `1,796,681`-byte content read and `204 delete`.
- GitHub Actions CI run `32330443414` passed at commit
  `a8b9e459dd1a0667486eee0fc3d6e077060a2804`: `validate` job
  `96309909355` and `container-smoke` job `96310016822` both completed with
  `success`, including Docker Compose build/start and `/health` smoke.
- GitHub Actions CI run `32331992481` passed at commit
  `14c65bf1773de0aa9cf5a8c4b1aaef2f47f5d8b0`: `validate` job
  `96314275300`, `redis-worker-smoke` job `96314275567` and `container-smoke`
  job `96314347912` all completed with `success`. The Redis job used a real
  Redis service and verified enqueue, ack, reconnect and abandoned-job reclaim.
- GitHub Actions CI run `32333068607` passed at commit
  `230138b4a6e88a170cb849a6d5a370885409ec6c`: `validate` job `96317282002`,
  `redis-worker-smoke` job `96317282174` and `container-smoke` job
  `96317378328` all completed with `success`. The scheduler E2E executed the
  allowlisted synthetic Agent, duplicate suppression, finding-only notification
  and cooperative cancellation.
- GitHub Actions CI run `32357057344` passed at commit
  `285ce62c15d04c27a7cbcef24e66d2f7404c3d55`: `validate` job
  `96388404384`, `redis-worker-smoke` job `96388404230` and `container-smoke`
  job `96388512618` all completed with `success`. The validation job included
  the privacy-safe OTLP trace tests and the Redis job used a real Redis service.
- GitHub Actions CI run `32437457413` passed at commit
  `52ccc13f7de777683555cce6c861ce8559d80480`: `validate` job
  `96641442671`, `redis-worker-smoke` job `96641442450` and `container-smoke`
  job `96641544862` all completed with `success`. This is the latest CI record
  before the scheduled-Agent worker path was added.
- GitHub Actions CI run `32440611630` passed at commit
  `5f3910aea8b54fe608fe7dc8d0d9cf7356bd2c4d`: `validate` job
  `96650415028`, `redis-worker-smoke` job `96650415169` and `container-smoke`
  job `96650500868` all completed with `success`. The Redis job exercised the
  scheduled-Agent queue payload, trusted identity boundary, runtime execution,
  distributed claim/completion dedupe and stream acknowledgement on a real
  GitHub-hosted Redis service.
- Browser E2E on `127.0.0.1:8767` completed through Chromium: bearer connect,
  grounded order-status run, PNG file chooser upload/preview, delete
  confirmation and empty-state refresh. The only console error was the
  expected initial unauthenticated dashboard `401`; favicon loading was fixed
  and produced no resource error.
- The in-app browser trusted-path runtime was unavailable, but the Playwright
  CLI Chromium run above provided local browser evidence.
- `pip-audit`: no known vulnerabilities in the resolved external dependencies;
  the unpublished local package is skipped because it is not in PyPI. The audit
  completed in the clean release venv after the OIDC, Redis, S3 and trace
  exporter production dependencies were installed; PyPI access was explicitly
  enabled for this check.
- Docker Compose configuration check: exit code `0`. The local Docker daemon
  was not used for a full production deployment.
- Wheel build and packaged-Web asset inspection: exit code `0`; the
  `0.2.0.dev0` wheel contains `apps/web`, attachments, Vision, approvals,
  metrics and durable-run code. A target-directory import smoke also found the
  packaged Web index. Final wheel SHA-256 is recorded below after the final
  build.

- Final local wheel SHA-256: `3f7de2e7c7cc67affe4a3a3d1f3f489cef88312dea82e067818a1736499324d1`
  for `enterprise_ai_copilot-0.2.0.dev0-py3-none-any.whl` built from commit
  `5f3910a`.

## Limits

This record does not prove production readiness, real ERP compatibility,
external provider behavior, or target-environment deployment. The local Docker
daemon and local Redis worker remain `NOT_RUN`; GitHub Actions Docker, Redis
adapter and scheduled-Agent E2E smokes are recorded separately as `PASS`, with
synthetic provider data and a development identity.

## Current release-track update: 2026-08-21

- Code commit: `98bf033f655ebd30776dd3e5b136f087d3b4dc9e`.
- Local suite: `65` tests passed; Ruff check/format, compileall, contracts,
  doctor, scheduler E2E, pip check, secret scan and production Compose config
  all passed. The first cached local wheel attempt hit Windows `WinError 5`;
  the no-cache wheel build passed and included `packages/agent_runtime/model.py`.
- GitHub Actions run `32442936627` passed: validate job `96657066535`,
  Redis worker job `96657066453`, CodeQL job `96657066265` and container-smoke
  job `96657160287`. The container scan used the immutable Trivy image digest
  `sha256:5c59e08f980b5d4d503329773480fcea2c9bdad7e381d846fbf9f2ecb8050f6b`.
- CI release artifact `9433062468` (`enterprise-ai-copilot-release-artifacts`)
  contains the wheel and SPDX package inventory; GitHub recorded artifact
  digest `sha256:0931ab21a2cfdac624ae584f3a32b652e0c4cc472d59f03927d43f8b07778ff8`.
- `python scripts/release_gate.py` returned exit code `1` because six formal
  target-environment checklist items remain unchecked. No target ERP/MCP,
  model, Vision/OCR, S3/ClamAV, backup/restore or production deployment call
  was made in this run.

## Current release-track update: 2026-08-21 (latest)

- Code commit: `d12a087b69496a4709a68cb9fbc2ece320cb73a7`; the worktree was
  clean after the commit and the commit was pushed to `main` and
  `codex/production-release` over HTTPS.
- Local suite: `70` tests passed; Ruff `0.15.12` check and format, compileall,
  repository validation, scheduler E2E, pip check and committed-history secret
  scan passed. The production preflight test verified fail-closed behavior for
  empty and placeholder environments.
- Hosted GitHub Actions run `32445379295` passed at this commit: validate
  `96663881954`, Redis worker smoke `96663882050`, CodeQL `96663882024` and
  container smoke `96663972605`. Container smoke included production Compose
  configuration and a production-image asset check.
- Local production image build passed from this commit using pinned Python base
  and Redis image references. Image manifest ID was
  `sha256:64535d440d04afb5be06e80e5dda4b252c49dd032a7de887db3c473a087c49c3`;
  the in-image check imported `services.scheduler` and found migrations,
  `scripts/production_preflight.py` and `scripts/connector_smoke.py`.
- The production profile now includes API, scheduler producer, continuous
  worker, Redis, PostgreSQL and a migration gate. PostgreSQL shared-state wiring
  and the target database restore path are locally smoke-tested; target-provider
  calls, target S3/ClamAV/Vision/OCR, failover, encrypted backup policy and a
  tagged release remain `UNVERIFIED` or `NOT_RUN`.

## Current release-track update: 2026-08-21 (PostgreSQL persistence)

- Code commit: `ee79a1524c8f7b1b4ac8f3b8e65f216713d8dbcb`.
- Local suite: `71` tests discovered, `70` passed and one opt-in PostgreSQL
  integration test was skipped without `AGENT_TEST_POSTGRES_URL`. Ruff check,
  format check, compileall, repository validation, pip check, secret scan,
  Docker Compose config and release-gate evaluation were run. The release gate
  correctly returned exit code `1` because six formal target gates remain open.
- Opt-in PostgreSQL integration passed against the pinned PostgreSQL 16 image:
  four migrations, all four scoped stores, cross-scope denial, idempotency,
  approval transition, health probes and duplicate idempotency return behavior.
- A custom-format `pg_dump` was restored into an isolated database with
  `pg_restore`; the restored database reported all four schema migrations.
  The host-specific `scripts/backup_postgres.py` wrapper was `NOT_RUN` because
  this Windows host does not have `pg_dump` on PATH.
- Production image build passed from this commit. Image manifest ID:
  `sha256:6476673b8e51dbb32e8523db6916903fe2c6e3c5f7f10915cea70dd9ccb8a56b`.
  The in-image check imported `psycopg` 3.3.4 and found scheduler,
  `migrate_postgres.py` and `backup_postgres.py` assets.
- Hosted GitHub Actions run `32449172148` passed at this commit: validate
  `96674249061`, CodeQL `96674249211`, Redis worker smoke `96674249223` and
  container smoke `96674329115`. This is CI evidence only; no target
  production environment or external provider was used.

## Current release-track update: 2026-08-21 (tenant-scoped audit persistence)

- Code commit: `128296424a8409f13c7379fba61aeb580d266bb9`; pushed to `main` and
  `codex/production-release` over HTTPS. The working tree was clean after the
  commit.
- Local suite: `78` tests passed and one opt-in PostgreSQL integration test was
  skipped in the default run; Ruff check/format, compileall, repository
  validation, scheduler E2E, pip check, committed-history secret scan and
  `git diff --check` passed. The default `production_preflight` invocation
  remains a documented FAIL because no target environment secrets/endpoints
  are present on this development host.
- Opt-in PostgreSQL integration passed against pinned PostgreSQL 16 digest
  `sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685`:
  five migrations, tenant-scoped audit reads, all scoped stores, health
  probes, cross-scope denial, approval transition and idempotency behavior.
  A custom-format `pg_dump` restored into an isolated database and reported
  `run_events=2`.
- Production image build passed as `enterprise-ai-copilot:production-track-v4`.
  Image manifest: `sha256:a134df6deeedb3c474a60a6ede6293262568a306f2a531549a1d0efa76097e49`.
  The in-image check imported `psycopg` `3.3.4` and `packages.scheduler`, and
  found production preflight, connector smoke, migration, backup and tenant
  scope migration assets.
- GitHub Actions run `32451104583` passed: validate `96679656392`, Redis
  worker smoke `96679656340`, CodeQL `96679656263` and container smoke
  `96679769463`. Hosted PostgreSQL integration and backup recovery smoke are
  synthetic CI evidence; target provider, S3, ClamAV, Vision/OCR, failover and
  production deployment evidence remain separate gates.

## Current release-track update: 2026-08-21 (portable release gate)

- Follow-up commit: `d9f528d7a16c2c3665f17d51aa7a076fa0c190c0`; pushed to
  `main` and `codex/production-release`. The release gate now passes an
  explicit safe-directory configuration to Git ancestry checks, so local
  owner-mismatch behavior matches the hosted runner.
- Local suite: `78` tests passed, Ruff check/format passed, and
  `python scripts/release_gate.py` correctly reports only the six unresolved
  target-environment checklist items; no stale evidence-binding errors remain.

## Current release-track update: 2026-08-21 (connector provenance and readiness)

- Code commit: `e01b09b06b19ec028c0e7b7b6dc74ef3cbd6b82a`.
- Local checks: `python -m unittest discover -s tests -q` passed `83` tests
  with one opt-in PostgreSQL integration test skipped; Ruff check and format
  check, compileall, `services.cli validate`, `services.cli doctor`, committed
  history secret scan and production Compose config all exited `0`.
- New contract tests cover MCP result scope/record identity, POST-only MCP
  readiness (`405`), attachment blob/scanner readiness and the read-only S3
  preflight probe.
- GitHub Actions run `32453472156` passed validate `96686162328`, Redis worker
  smoke `96686162226`, CodeQL `96686162397` and container smoke `96686291474`.
  The uploaded `enterprise-ai-copilot-release-artifacts` artifact is `9436432361`.
- This evidence does not prove target OIDC, ERP/MCP, model, Vision/OCR, S3,
  ClamAV, OTLP, backup/restore, failover or production deployment behavior.

## Current release-track update: 2026-08-21 (release hardening)

- Code commit: `5be8584af739235702fabd2e779cdaf3b3b9d1b7` on the isolated
  `codex/production-hardening` branch; no release tag was created.
- Local suite: `89` tests passed with one opt-in PostgreSQL integration test
  skipped. Ruff check, format check, compileall, `services.cli validate`,
  `services.cli doctor`, committed-history secret scan, production Compose
  configuration with the same `--project-directory .` invocation used by CI,
  and `git diff --check` passed.
- New contract tests reject oversized remote MCP provenance fields and
  oversized connector status values; the web console now escapes the dynamic
  tool count, and the release workflow checks that a stable `vMAJOR.MINOR.PATCH`
  tag exactly matches the package version.
- `python scripts/release_gate.py` returned exit code `1` with exactly six
  formal target-environment checklist items still open. The intentional
  `python scripts/check_release_version.py --tag v0.2.0` check also failed
  closed because the package is still `0.2.0.dev0`; no GitHub release was
  published.
- This local evidence does not prove target OIDC, ERP/MCP, model, Vision/OCR,
  S3, ClamAV, OTLP, backup/restore, failover, hosted CI for this commit or
  production deployment behavior.

## Current hosted validation update: 2026-08-21

- GitHub Actions run `32455021819` for commit `5644d8eb1c06346c421529daa923ff4ad11a173a`
  completed with conclusion `success`.
- The `validate` job `96690535071`, Redis worker smoke job `96690534785`,
  CodeQL job `96690534958` and container smoke job `96690712916` all passed.
  The workflow produced the `enterprise-ai-copilot-release-artifacts` artifact
  `9436961479`.
- This is hosted CI and synthetic/provider-boundary evidence. It does not
  prove target OIDC, ERP/MCP, model, Vision/OCR, S3, ClamAV, OTLP,
  backup/restore, failover or production deployment behavior.

## Current release-track update: 2026-08-21 (protected acceptance path)

- Code commit: `dcd61b2cbece5fdbedf459224124bd880a410522`, pushed to
  `main`, `codex/production-release` and `codex/production-hardening`.
- Local suite: `96` tests passed with one opt-in PostgreSQL integration test
  skipped. Ruff check/format, compileall, repository validation, doctor,
  committed-history secret scan, production Compose configuration, workflow
  YAML parsing and `git diff --check` passed.
- The fake-client acceptance tests passed the two-principal scope assertion,
  ERP run isolation, attachment owner/cross-scope checks and fixture cleanup.
  The live protected acceptance workflow was not run because this checkout has
  no target credentials or deployed API.
- GitHub Actions run `32457142256` completed successfully: validate
  `96696499134`, Redis worker smoke `96696499017`, CodeQL `96696498819` and
  container smoke `96696648000`. Release artifact `9437630755` was retained.
- This remains repository and hosted synthetic evidence. Target OIDC, ERP/MCP,
  model, Vision/OCR, S3, ClamAV, OTLP, backup/restore, failover and production
  deployment evidence remain `NOT_RUN` or `UNVERIFIED`.

## Current release-track update: 2026-08-21 (storage and target acceptance hardening)

- Code commit: `8e240238a156cc4e72116ac92e665b97b81e6a9b` on
  `codex/production-hardening`; it contains no credentials or target data.
- Local suite: `101` tests passed with one opt-in PostgreSQL integration test
  skipped. Ruff check/format, compileall, repository validation, doctor,
  workflow YAML parsing and `git diff --check` passed.
- The S3 adapter now verifies `HeadObject` server-side encryption metadata after
  each write and best-effort deletes an object when encryption or KMS metadata
  cannot be verified. Unit tests cover mismatch, missing KMS metadata and
  cleanup of the unverified object.
- The protected acceptance workflow now includes a temporary PostgreSQL
  backup/isolated-restore smoke and a real-Redis reconnect/reclaim smoke. The
  dump is not uploaded. The target workflow remains `NOT_RUN` because this
  checkout has no target credentials or deployed API; cancellation/retry and
  all external target-provider evidence remain `NOT_RUN` or `UNVERIFIED`.

## Current hosted validation update: 2026-08-21 (storage and target acceptance hardening)

- Draft PR `#5` ran GitHub Actions CI for exact commit
  `57f1bf965cc5ac6a7b6f5df943ed457576790c25`.
- Run `32459127807` completed with `success`: validate `96702306411`, Redis
  worker smoke `96702306581`, CodeQL `96702306242` and container smoke
  `96702470699`.
- The retained `enterprise-ai-copilot-release-artifacts` artifact is
  `9438308288`. This hosted run uses synthetic/provider-boundary fixtures and
  disposable PostgreSQL/Redis services; it does not prove target OIDC,
  ERP/MCP, model, Vision/OCR, S3, ClamAV, OTLP, backup/restore, failover or
  production deployment behavior.

## Current local validation update: 2026-08-21 (durable schedule cancellation)

- Code commit: `d8e78b88947a942dacb52dac7e4b9cf5dfc277e1`.
- `python -m unittest discover -s tests -q`: `PASS` — 104 tests passed and
  one explicitly opt-in PostgreSQL integration test was skipped.
- `python -m ruff check packages services tests scripts`: `PASS`;
  `python -m ruff format --check packages services tests scripts`: `PASS`;
  `python -m compileall -q packages services scripts tests apps`: `PASS`;
  manifest validation and `services.cli doctor`: `PASS`; committed-history
  secret scan: `PASS`; production Compose config using
  `docker compose --project-directory . -f deploy/docker-compose.production.yml
  config --quiet`: `PASS`; `git diff --check`: `PASS`.
- The new Redis queue contract uses an atomic, short-lived cancellation marker;
  worker and Agent Runtime tests verify that queued/retrying work and the next
  external operation are stopped cooperatively. The operator CLI requires
  `--confirm-cancel` and does not print credentials or payloads.
- `python scripts/release_gate.py`: `FAIL` with exactly six unresolved formal
  target-environment gates. This is the intended fail-closed result; no target
  credentials, deployed API, real external providers or production host were
  used by these local checks.

## Current hosted validation update: 2026-08-21 (durable schedule cancellation)

- GitHub Actions run `32460764610` for exact commit
  `d8e78b88947a942dacb52dac7e4b9cf5dfc277e1` completed with `success`:
  validate `96707076413`, Redis worker smoke `96707076406`, CodeQL
  `96707076165` and container smoke `96707247423` all completed with
  `success`.
- Release artifact `enterprise-ai-copilot-release-artifacts` is
  `9438859953` and was not expired when observed. The hosted Redis smoke
  includes the synthetic cancellation-marker assertion.
- This remains hosted CI and synthetic/provider-boundary evidence. It does not
  prove target Redis failover, target cancellation under load, OIDC, ERP/MCP,
  model, Vision/OCR, S3, ClamAV, OTLP, backup/restore, failover or production
  deployment behavior.

## Current local validation update: 2026-08-21 (approval binding and idempotency hardening)

- Code commit: `01919fcbfb130c0be21c0f8607ad36ebdc7773f4` on
  `codex/production-hardening`.
- `python -m unittest discover -s tests -q`: `PASS` — 106 tests passed and
  one explicitly opt-in PostgreSQL integration test was skipped.
- Ruff check, Ruff format check, compileall, manifest validation,
  `services.cli doctor`, committed-history secret scan, production Compose
  configuration and `git diff --check`: `PASS`.
- Approval verification is now fail-closed and runtime-bound to the durable
  `ApprovalService`; high-risk authorization requires exact identity/tool/scope/
  argument/token matching and atomically consumes the approval so replay fails.
  SQLite duplicate run saves also return the existing scoped idempotent record
  after a uniqueness race.
- `python scripts/release_gate.py`: `FAIL` with exactly six unresolved formal
  target-environment checklist items. This is the intended fail-closed result;
  no target credentials, deployed API, real external providers or production
  host were used by these local checks. The intentional
  `python scripts/check_release_version.py --tag v0.2.0` check remains closed
  because the package is still `0.2.0.dev0`; no release tag was created.
- A fresh browser smoke for this commit is `NOT_RUN` because the local
  Playwright wrapper could not initialize in this Windows environment. Earlier
  recorded browser evidence remains historical and is not reclassified as a
  current-commit result.

## Current hosted validation update: 2026-08-21 (approval binding and idempotency hardening)

- GitHub Actions run `32463024508` for exact commit
  `01919fcbfb130c0be21c0f8607ad36ebdc7773f4` completed with `success`.
- The `validate` job `96713701443`, Redis worker smoke job `96713701298`,
  CodeQL job `96713701435` and container smoke job `96713864379` all passed.
  The workflow produced the retained
  `enterprise-ai-copilot-release-artifacts` artifact `9439633190`.
- This is hosted CI and synthetic/provider-boundary evidence. It does not
  prove target OIDC, ERP/MCP, model, Vision/OCR, S3, ClamAV, OTLP,
  backup/restore, failover, browser acceptance or production deployment
  behavior.
