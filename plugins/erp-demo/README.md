# ERP Demo Plugin

This is a synthetic, read-only example Plugin. It contains no credentials and
does not connect to a real ERP. Installation is review-gated by the local
Plugin registry and does not execute package code.
